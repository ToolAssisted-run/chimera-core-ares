--- a/mia/system/mega-drive.cpp
+++ b/mia/system/mega-drive.cpp
@@ -5,9 +5,10 @@
 };
 
 auto MegaDrive::load(string location) -> LoadResult {
+  //No tmss.rom: it is Sega's, and this core never turns TMSS on, so the machine
+  //only ever read it into a buffer it did not map.
   this->location = locate();
   pak = std::make_shared<vfs::directory>();
-  pak->append("tmss.rom", Resource::MegaDrive::TMSS);
   return successful;
 }
 
--- a/mia/medium/mega-drive.cpp
+++ b/mia/medium/mega-drive.cpp
@@ -61,13 +61,10 @@
   pak->setAttribute("megacd",   (bool)(std::ranges::find(devices, string{"Mega CD"}) != devices.end()));
   pak->append("manifest.bml", manifest);
 
-  //add SVP ROM to image if it is missing
-  if(document["game/board/memory(type=ROM,content=SVP)"]) {
-    if(memory::compare(rom.data() + rom.size() - 0x800, Resource::MegaDrive::SVP, 0x800)) {
-      rom.resize(rom.size() + 0x800);
-      memory::copy(rom.data() + rom.size() - 0x800, Resource::MegaDrive::SVP, 0x800);
-    }
-  }
+  //A Virtua Racing cartridge carries an SVP chip whose 2KB ROM most dumps leave
+  //out, and mia used to splice in a compiled-in copy. It is Sega's, so it is
+  //gone, and that game needs a dump that already has it. The only capability
+  //this core lost by carrying no firmware but its own.
 
   std::span<const u8> view{rom};
   for(auto node : document.find("game/board/memory(type=ROM)")) {
