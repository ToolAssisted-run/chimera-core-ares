--- a/mia/system/mega-ld.cpp
+++ b/mia/system/mega-ld.cpp
@@ -12,24 +12,10 @@
 };
 
 auto MegaLD::load(string location) -> LoadResult {
-  auto bios = Pak::read(location);
-  if(bios.empty()) return romNotFound;
-
-  this->location = locate();
-  pak = std::make_shared<vfs::directory>();
-  pak->append("tmss.rom", Resource::MegaDrive::TMSS);
-  pak->append("bios.rom", bios);
-  pak->append("backup.ram", 8_KiB);
-
-  if(auto fp = pak->write("backup.ram")) {
-    for(auto address : range(fp->size())) fp->write(0xff);
-    fp->seek(fp->size() - sizeof(bram));
-    for(auto& byte : bram) fp->write(byte);
-  }
-
-  Pak::load("backup.ram", ".bram");
-
-  return successful;
+  //This core offers no such machine, and the firmware it would need is not ours
+  //to carry. Refused rather than half-built: a pak missing its boot ROM makes a
+  //machine that runs zeros.
+  return romNotFound;
 }
 
 auto MegaLD::save(string location) -> bool {
--- a/mia/medium/super-famicom.cpp
+++ b/mia/medium/super-famicom.cpp
@@ -39,26 +39,15 @@ auto SuperFamicom::load(string location) -> LoadResult {
   bool local_firmware = false;
   bool folder = false;
   
+  //A cartridge whose coprocessor ROM is missing used to have it supplied from a
+  //compiled-in copy - eleven of them, all somebody else's. They are not ours to
+  //carry, so a cartridge that needs one has to bring it, and until the package
+  //can say "this firmware only for some cartridges" such a cartridge is
+  //refused. Nothing here calls this any more; it is left as the seam it would
+  //go back into.
   auto append_missing_firmware = [&](auto document)
   {
-    if(auto identifier = document["game/board/memory/identifier"]) {
-      if(!firmwareRomSize() && !local_firmware) {
-        auto id = identifier.string();
-        std::span<const u8> view;
-        if(id == "Cx4"  ) view = Resource::SuperFamicom::Cx4;
-        if(id == "DSP1" ) view = Resource::SuperFamicom::DSP1;
-        if(id == "DSP1B") view = Resource::SuperFamicom::DSP1B;
-        if(id == "DSP2" ) view = Resource::SuperFamicom::DSP2;
-        if(id == "DSP3" ) view = Resource::SuperFamicom::DSP3;
-        if(id == "DSP4" ) view = Resource::SuperFamicom::DSP4;
-        if(id == "SGB1" ) view = Resource::SuperFamicom::SGB1;
-        if(id == "SGB2" ) view = Resource::SuperFamicom::SGB2;
-        if(id == "ST010") view = Resource::SuperFamicom::ST010;
-        if(id == "ST011") view = Resource::SuperFamicom::ST011;
-        if(id == "ST018") view = Resource::SuperFamicom::ST018;
-        for(auto byte : view) rom.push_back(byte);
-      }
-	}
+    (void)document;
   };
   
   if(directory::exists(location)) {
--- a/mia/system/pocket-challenge-v2.cpp
+++ b/mia/system/pocket-challenge-v2.cpp
@@ -5,11 +5,10 @@
 };
 
 auto PocketChallengeV2::load(string location) -> LoadResult {
-  this->location = locate();
-  pak = std::make_shared<vfs::directory>();
-  pak->append("boot.rom", Resource::PocketChallengeV2::Boot);
-
-  return successful;
+  //This core offers no such machine, and the firmware it would need is not ours
+  //to carry. Refused rather than half-built: a pak missing its boot ROM makes a
+  //machine that runs zeros.
+  return romNotFound;
 }
 
 auto PocketChallengeV2::save(string location) -> bool {
--- a/mia/system/zx-spectrum-128.cpp
+++ b/mia/system/zx-spectrum-128.cpp
@@ -5,11 +5,10 @@
 };
 
 auto ZXSpectrum128::load(string location) -> LoadResult {
-  this->location = locate();
-  pak = std::make_shared<vfs::directory>();
-  pak->append("bios.rom", Resource::ZXSpectrum128::BIOS);
-  pak->append("sub.rom", Resource::ZXSpectrum128::Sub);
-  return successful;
+  //This core offers no such machine, and the firmware it would need is not ours
+  //to carry. Refused rather than half-built: a pak missing its boot ROM makes a
+  //machine that runs zeros.
+  return romNotFound;
 }
 
 auto ZXSpectrum128::save(string location) -> bool {
